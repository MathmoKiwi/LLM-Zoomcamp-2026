apiVersion: 1

datasources:
  - name: TechnotePG
    # Fixed uid so the provisioned dashboard can reference it.
    uid: technotepg
    type: postgres
    access: proxy
    url: postgres:5432
    database: technote
    user: technote
    secureJsonData:
      password: technote
    jsonData:
      sslmode: disable
      postgresVersion: 1600
